name: "CI Publish"

on:
  workflow_run:
    workflows:
      - "CI"
    types:
      - completed

jobs:
  test-results:
    name: "Test Results"
    if: ${{ github.event.workflow_run.conclusion != 'skipped' }}
    runs-on: ubuntu-latest

    permissions: { }

    steps:
      - name: "TODO"
        run: 'true'
